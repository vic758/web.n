<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Entrepreneur Hub</title>
<link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">

<style>
* { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Roboto', sans-serif; }
body { background:#f4f4f4; color:#333; line-height:1.6; }
header { background:linear-gradient(90deg,#4CAF50,#45a049); color:#fff; text-align:center; padding:2rem; }
nav a { color:white; margin:0 1rem; font-weight:bold; }
section { padding:4rem 1rem; max-width:1000px; margin:auto; text-align:center; }
.hero { background:#fff; }
.services,.testimonials { display:flex; flex-wrap:wrap; justify-content:center; gap:2rem; }
.service-card,.testimonial-card {
  background:white; padding:2rem; width:300px;
  border-radius:10px; box-shadow:0 5px 15px rgba(0,0,0,.1);
}
footer { background:#333; color:white; text-align:center; padding:2rem; }

canvas {
  display:block;
  margin:2rem auto;
  background:#111;
  border-radius:10px;
  box-shadow:0 0 20px #4CAF50;
}
</style>
</head>

<body>

<header>
<h1>Entrepreneur Hub</h1>
<nav>
<a href="#services">Services</a>
<a href="#newsletter">Newsletter</a>
<a href="#testimonials">Testimonials</a>
<a href="#game">Game</a>
</nav>
</header>

<section class="hero">
<h2>Grow Your Business & Monetize Smartly</h2>
<p>Learn, grow, and have fun while doing it.</p>
</section>

<section id="services">
<h2>Our Services</h2>
<div class="services">
<div class="service-card">Product Launch</div>
<div class="service-card">Marketing Strategy</div>
<div class="service-card">Monetization Plan</div>
</div>
</section>

<section id="game">
<h2>🎮 Play Neon Dodge</h2>
<p>Use ← → or A D to move</p>
<canvas id="gameCanvas" width="400" height="600"></canvas>
</section>

<section id="testimonials">
<h2>Testimonials</h2>
<div class="testimonials">
<div class="testimonial-card">Amazing results!</div>
<div class="testimonial-card">Very helpful!</div>
</div>
</section>

<footer>
<p>&copy; 2025 Entrepreneur Hub</p>
</footer>

<script>
const canvas = document.getElementById("gameCanvas");
const ctx = canvas.getContext("2d");

let player = { x:180, y:520, size:40, speed:6 };
let enemies = [];
let score = 0;
let gameOver = false;

document.addEventListener("keydown", e => {
  if(e.key==="ArrowLeft"||e.key==="a") player.x -= player.speed;
  if(e.key==="ArrowRight"||e.key==="d") player.x += player.speed;
});

function spawnEnemy(){
  enemies.push({
    x:Math.random()*(canvas.width-40),
    y:-40,
    size:40,
    speed:3+score*0.02
  });
}

function gameLoop(){
  if(gameOver){
    ctx.fillStyle="white";
    ctx.font="30px Arial";
    ctx.fillText("GAME OVER",120,300);
    return;
  }

  ctx.clearRect(0,0,canvas.width,canvas.height);

  ctx.fillStyle="#4CAF50";
  ctx.fillRect(player.x,player.y,player.size,player.size);

  ctx.fillStyle="red";
  enemies.forEach(e=>{
    e.y+=e.speed;
    ctx.fillRect(e.x,e.y,e.size,e.size);
    if(
      e.x<player.x+player.size &&
      e.x+e.size>player.x &&
      e.y<player.y+player.size &&
      e.y+e.size>player.y
    ){ gameOver=true; }
  });

  enemies=enemies.filter(e=>e.y<canvas.height);
  score++;
  ctx.fillStyle="white";
  ctx.fillText("Score: "+score,10,20);

  if(Math.random()<0.03) spawnEnemy();
  requestAnimationFrame(gameLoop);
}

gameLoop();
</script>

</body>
</html>
